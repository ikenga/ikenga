apply plugin: 'java'


dependencies {
    compile(
            libraries.ikengaModule,
            project(":server:ikenga-persistence")
    )

    testCompile(
            project(":server:ikenga-test"),

    )
}